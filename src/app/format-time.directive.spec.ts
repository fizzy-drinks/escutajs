import { FormatTimeDirective } from './format-time.directive';

describe('FormatTimeDirective', () => {
  it('should create an instance', () => {
    const directive = new FormatTimeDirective();
    expect(directive).toBeTruthy();
  });
});
